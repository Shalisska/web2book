mixin inset_paper_inner(name, items)
	-var itemName = pageClass + '__ruler'
	.calculator__item.form__ruler(class=itemName + ' ' + name)
		+form_ruler(itemName, name, items, 3)

mixin inset_paper(name)
	+inset_paper_inner(name, [70, 80, 130, 150, 170, 200, 250, 300])