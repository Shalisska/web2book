mixin calculator-types(form_name, items)
	each item, i in items
		if i===3
			input(type='radio' class=form_name + '__input' name=form_name id=form_name + '--' + item.id checked)
		else
			input(type='radio' class=form_name + '__input' name=form_name id=form_name + '--' + item.id)
	
	div(class=form_name + '__controls')
		each item in items
			label(class=form_name + '__controls-item' for=form_name + '--' + item.id)
				div(class=form_name + '__controls-item_img' + ' ' + form_name + '__controls-item_img--' + item.id)
					+picture({
						name: item.name,
						adress: '../images/calculator/types/calculator-types@',
						ext: 'png'
					}, {
						main: {
							width: 700,
							img: 700
						}
					})
				p(class=form_name + '__controls-item_name')=item.name
	div(class=form_name + '__scroll')
		div(class=form_name + '__scrollbar')
			p(class=form_name + '__thumb') Типы продукции



+calculator-types('calculator-types', [{
			id: 'softcover2',
			name: 'Мягкий переплет'
		}, {
			id: 'softcover1',
			name: 'Мягкий переплет'
		}, {
			id: 'softcover',
			name: 'Мягкий переплет'
		}, {
			id: 'spring',
			name: 'Пружина'
		}, {
			id: 'strap',
			name: 'Скоба'
		}, {
			id: 'strap1',
			name: 'Скоба'
		}, {
			id: 'strap2',
			name: 'Скоба'
		}])