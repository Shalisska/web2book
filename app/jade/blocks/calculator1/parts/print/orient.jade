mixin print_orient(name, items)
	.calculator__item.calculator__scroller(class=name)
		+calc_scroller(name, items)
		div(class=name + '-desc')
			each item in items
				div(class=name + '-desc__item')
					if item.desc
						span(class=name + '-desc__item-text')=item.desc
					img(class=name + '-desc__item-img' src='' alt=item.name)





mixin orient(name)
	+print_orient(name, [{
		id: 'book',
		val: 'book',
		name: 'Книжная'
	}, {
		id: 'album',
		val: 'album',
		name: 'Альбомная'
	}])